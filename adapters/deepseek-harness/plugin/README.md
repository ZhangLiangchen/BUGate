# DeepSeek Harness Plugin Integration

Reserved for HyperTest.

The future plugin will adapt DeepSeek Harness lifecycle events to the same
`PreparedProtocolContext` contract used by other BUGate hosts. DSH-specific
runtime APIs must not enter BUGate Protocol Core.

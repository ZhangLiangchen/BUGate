# Pi Package Integration

Reserved for HyperTest.

The future package/extension will adapt Pi lifecycle events to the same
`PreparedProtocolContext` contract used by Claude Code and Codex. Pi-specific
session/tool APIs must not enter BUGate Protocol Core.
